{
  "name": "wander-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:shared": "cd packages/shared && npm run build",
    "install-all": "npm install && cd apps/frontend && npm install",
    "dev": "concurrently \"cd apps/backend && python main.py\" \"cd apps/frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}